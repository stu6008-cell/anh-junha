# 어셈블리어 정리

어셈블리어(Assembly Language)는 컴퓨터의 뇌인 CPU가 직접 이해하는 '기계어(0과 1)'를 사람이 읽을 수 있는 문자 형태로 1:1 대응시킨 저급(Low-level) 프로그래밍 언어다.

컴퓨터 하드웨어를 직접 제어할 수 있는 가장 근원적인 언어이며, 작성된 코드는 어셈블러(Assembler)라는 변환 도구를 거쳐 컴퓨터가 실행할 수 있는 기계어로 번역된다.

## 1. 레지스터

어셈블리에는 변수가 없다. 그 대신 메모리 주소와 실제 상수를 담는 **레지스터**라는 것이 있다.

`rax`, `rbx`, `rcx`, `rdx` ... 이런 것들이다.

## 2. 넣기 / 계산하기

```asm
mov rax, 5     ; rax 서랍에 5 넣어
add rax, 3     ; rax에 3 더해   → 이제 8
sub rax, 1     ; rax에서 1 빼   → 이제 7
```

`mov`는 move, `add`는 더하기, `sub`는 빼기. 그냥 영어 줄임말이다.

## 3. 쪼개기

```c
int a = 5 + 3;
```

이 C 코드를 어셈블리로 바꾸면:

```asm
mov rax, 5
add rax, 3
mov [a], rax     ; a라는 공간에 결과 저장
```

어셈블리의 전부라고 해도 될 정도다 — 잘게 쪼개는 것.

## 4. 점프

어셈블리어에는 `if`나 `while` 문이 없다. 그 대신 **jump**가 있다.

- `if`는 비교하고 뛰어넘기로 만들고
- 반복문도 그냥 위로 다시 점프하면 그게 반복문이다

점프 종류:

| 명령어 | 의미 |
|---|---|
| `je`  | 같으면 점프 (equal) |
| `jne` | 다르면 점프 |
| `jg`  | 크면 점프 (greater) |
| `jl`  | 작으면 점프 (less) |
| `jmp` | 무조건 점프 |

반복:

```asm
반복:
    dec rcx        ; rcx 1 줄여
    cmp rcx, 0
    jg  반복        ; 아직 0보다 크면 위로 돌아가
```

비교:

```asm
cmp rax, 10    ; rax랑 10 비교해봐
je  같음        ; 같으면 '같음' 위치로 점프
               ; 안 같으면 그냥 다음 줄로 계속
```

## 5. 대괄호

```asm
mov rax, myVar      ; myVar가 있는 '위치'
mov rax, [myVar]    ; myVar에 '들어있는 값'
```

집 주소 vs 집 안에 있는 사람. 이 차이다.

## 6. 메모리 — 서랍이 모자랄 때

CPU 안의 서랍(레지스터)은 십몇 개뿐이다. 그것보다 많은 걸 저장하려면 큰 창고(메모리)에 둔다.

```nasm
section .data      ; 값 정해서 넣어두는 칸
    num dq 100     ; 여기에 100 넣어둠

section .bss       ; 빈 칸만 잡아두기
    temp resq 1    ; "자리 하나 예약" (값은 나중에)
```

크기 이름은 그냥 "얼마나 큰 칸이냐"다:

| 이름 | 크기 |
|---|---|
| `db` | 작음 (1바이트) |
| `dw` | 조금 (2바이트) |
| `dd` | 보통 (4바이트, int) |
| `dq` | 큼 (8바이트) |

지금은 `dd`(int 크기) 하나만 기억해도 충분하다.

## 7. 배열 — 그냥 붙어있는 칸들

배열은 특별한 게 아니라 칸을 줄줄이 붙여놓은 것뿐이다.

```nasm
arr dd 10, 20, 30, 40    ; 칸 4개: [10][20][30][40]
```

`arr[2]`(=30)를 꺼내려면? 시작 위치에서 2칸 건너뛰기.

```asm
mov eax, [arr + rbx*4]   ; rbx가 2면 → arr[2] = 30
```

`[시작 + 몇번째 × 칸크기]` — 이게 배열 꺼내기의 전부다. C에서 `arr[2]` 쓸 때 컴퓨터가 속으로 이 계산을 하는 것이다.

## 8. 스택 — 잠깐 맡기는 곳

서랍이 부족할 때 잠깐 쌓아두는 곳이다. 접시 쌓기랑 똑같다.

```asm
push rax    ; 접시 하나 얹음
push rbx    ; 그 위에 또 얹음
pop rbx     ; 위에서부터 꺼냄
pop rax     ; 그 밑에 거 꺼냄
```

핵심: 나중에 얹은 걸 먼저 꺼낸다. (접시 맨 밑에 거 먼저 못 빼니까.) 그래서 `push` 순서랑 `pop` 순서가 반대다.

## 9. 함수 — 심부름 시키기

```asm
mov rdi, 5        ; "5 줄게"
call my_func      ; "이거 처리해줘"
; 끝나면 답이 rax에 옴

my_func:
    mov rax, rdi      ; 받은 5
    add rax, 10       ; +10 → 15
    ret               ; "답 여기 있어" 하고 돌아감
```

- `call` = "이거 해줘" 하고 넘김 (돌아올 길도 기억해둠)
- `ret` = 일 끝내고 돌아옴

약속(규칙): 심부름 시킬 때 재료는 정해진 서랍에 담는다.

- 첫 번째 인자 → `rdi`
- 두 번째 인자 → `rsi`
- 답은 항상 → `rax`

이건 문법이 아니라 "다들 이렇게 하기로 한 약속"이다.

## 10. 프롤로그 / 에필로그 — 함수의 정형구

함수 앞뒤에 항상 붙는 부분이다.

```asm
push rbp          ; 책상 갖고 옴 (준비)
mov rbp, rsp      ; 책상 놓음
sub rsp, 32       ; 작업 공간 확보

; ... 여기서 일함 ...

mov rsp, rbp      ; 공간 치움
pop rbp           ; 책상 치움 (정리)
ret               ; 돌아감
```

`[rbp-8]` 이런 게 지역변수다. 함수 끝나면 공간을 통째로 치워버리니까 (그래서 함수 밖에선 안 보인다). "책상 위에 올려둔 건 일 끝나면 다 치운다"는 느낌.

## 11. 실전 — 배열 다 더하기

C로는 한 줄이다:

```c
for (int i = 0; i < 5; i++) sum += arr[i];
```

어셈블리로는:

```asm
mov eax, 0        ; 합계 = 0
mov rbx, 0        ; i = 0

loop_start:
    cmp rbx, 5            ; i가 5냐?
    jge loop_end          ; 그럼 끝내
    add eax, [arr+rbx*4]  ; 합계에 arr[i] 더해
    inc rbx               ; i++
    jmp loop_start        ; 위로 다시

loop_end:
    ; eax에 다 더한 값
```

어셈블리엔 `for`문이 없다. 대신 "확인하고 → 더하고 → 위로 다시 점프". 이게 반복문의 정체다.

## 12. 논리 연산 — 딱 하나만

나머지는 나중에 필요할 때 보고, 이거 하나만 기억하면 된다:

```asm
xor rax, rax    ; rax = 0
```

자기 자신이랑 XOR하면 무조건 0이 된다. `mov rax, 0`보다 빨라서, 프로그램을 뜯어보면 "0으로 만들기"를 이걸로 하는 걸 진짜 자주 본다. 그래서 이거 보면 "아, 0으로 초기화하는구나" 하고 알아채면 된다.

## 13. 8086 버전 — EasyCPU에서 그대로 쳐보기

EasyCPU는 64비트 x86-64가 아니라 옛날 16비트 8086이다. 레지스터 이름과 크기가 다를 뿐, mov/add/sub·jump·스택 같은 핵심 개념은 거의 그대로 넘어간다.

레지스터 이름표:

| 위(x86-64) | 8086 |
|---|---|
| `rax` | `ax` |
| `rbx` | `bx` |
| `rcx` | `cx` |
| `rdx` | `dx` |
| `rsi` | `si` |
| `rdi` | `di` |
| `rbp` | `bp` |
| `rsp` | `sp` |

8086 레지스터는 전부 16비트뿐이다 (32비트 `eax`, 64비트 `rax`는 한참 나중에 생긴 것들이다).

**2. 넣기/계산하기** — 레지스터 이름만 바뀐다:

```asm
mov ax, 5     ; ax 서랍에 5 넣어
add ax, 3     ; ax에 3 더해   → 이제 8
sub ax, 1     ; ax에서 1 빼   → 이제 7
```

**4. 점프** — `je`, `jne`, `jg`, `jl`, `jmp`는 완전히 똑같다. `rcx` 대신 `cx`만 쓰면 된다.

**7. 배열 — 여기가 진짜 다른 부분이다**

원래 노트의 `[arr + rbx*4]`처럼 인덱스에 자동으로 곱셈(×4)을 해주는 주소 계산은 8086에는 없다. 그래서 인덱스 대신 "몇 바이트 떨어졌는지"를 직접 더해가며 써야 한다:

```asm
mov bx, 0          ; 오프셋(바이트 수), 인덱스 자체가 아님
mov ax, [arr+bx]   ; arr[0]
add bx, 2          ; word(2바이트)짜리 칸이니까 +2 해야 다음 칸
mov ax, [arr+bx]   ; arr[1]
```

**8. 스택** — 그대로다, 레지스터만 16비트로:

```asm
push ax
push bx
pop bx
pop ax
```

**9. 함수** — x86-64는 "1번째 인자는 rdi, 2번째는 rsi, 답은 rax"라는 정해진 규칙이 있었지만, 8086에는 그런 공식 규칙이 없다. 아무 레지스터나 골라서 스스로 정하면 되고, 보통 `ax`를 많이 쓴다:

```asm
mov ax, 5         ; "5 줄게" (ax에 담기로 내가 정함)
call my_func

my_func:
    add ax, 10        ; +10 → 15
    ret               ; 답(ax)은 그대로 들고 돌아감
```

**10. 프롤로그/에필로그** — 개념도 코드도 거의 그대로, 레지스터만 16비트:

```asm
push bp
mov bp, sp
sub sp, 4

; ... 여기서 일함 ...

mov sp, bp
pop bp
ret
```

**11. 실전 — 배열 합 (EasyCPU에 바로 붙여넣는 버전)**

```asm
mov ax, 0        ; 합계 = 0
mov bx, 0        ; 오프셋 = 0
mov cx, 0        ; i = 0

loop_start:
    cmp cx, 5             ; i가 5냐?
    jge loop_end          ; 그럼 끝내
    add ax, [arr+bx]      ; 합계 += arr[i]
    add bx, 2             ; 오프셋 +2 (word 크기)
    inc cx                ; i++
    jmp loop_start        ; 위로 다시

loop_end:
    ; ax에 다 더한 값
```

**12. xor** — 그대로다: `xor ax, ax` → `ax = 0`.

> EasyCPU 사이트 자체는 접속이 막혀 있어서 배열 선언 줄 같은 세부 문법까지는 직접 확인하지 못했다. 위 명령어들(`mov`/`add`/`sub`/`cmp`/`je`·`jg`·`jmp` 계열/`push`/`pop`/`call`/`ret`)은 8086 표준이라 대부분의 8086 시뮬레이터에서 그대로 동작하지만, `arr`처럼 데이터를 선언하는 줄만은 EasyCPU 화면의 예제 코드를 한 번 보고 그 형식에 맞춰 쓰면 된다.
